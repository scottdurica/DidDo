<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.emroxriprap.diddo.inputFragments.AtFragment"
    android:orientation="vertical">


    <TextView
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:id="@+id/textView3"
        android:layout_gravity="center_horizontal"
        android:text="@string/at"
        android:textColor="@color/primary"
        android:textSize="@dimen/large_text"
        android:textStyle="bold"
        android:layout_weight=".065"
        android:layout_marginTop="@dimen/large_text_padding" />

    <CheckBox
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:text="@string/cb_all_day_event"
        android:id="@+id/cb_all_day_event"
        android:checked="true"
        android:layout_margin="@dimen/ten"
        android:layout_weight=".03" />

    <TimePicker
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:id="@+id/timePicker"
        android:layout_gravity="center_horizontal"
        android:timePickerMode="clock"
        android:layout_weight=".55" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/fab_next"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:src="@drawable/ic_arrow_forward_white_24dp"
        app:elevation="4dp"
        android:layout_gravity="right|bottom"
        android:layout_marginRight="10dp"
        android:layout_marginBottom="10dp"
        android:layout_weight=".1" />
</LinearLayout>
